Vendored files previously used by libtcod.  These are being phased out.

While these libraries are still used by libtcod, most of these sources are ignored by CMake builds.

Some other projects might will rely on them though, such as `python-tcod`.
